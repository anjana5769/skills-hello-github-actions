name: Post welcome comment

on:
  pull_request:
    types: [opened]
  
  
jobs:
  welcome-job:
    runs-on: [ubuntu-latest]
    permissions:
      pull-requests: write    
    steps:
        - name: First interaction
          uses: actions/first-interaction@v1
          with:
            repo-token: ${{ secrets.GITHUB_TOKEN }}
            issue-message: "👋 Thanks for your first issue! We’ll review it soon."
            pr-message: "🎉 Thanks for your first PR! A maintainer will review shortly."

